using UnityEngine;

public class circle_color_changer : MonoBehaviour
{

    // Gameobjects for the circle
    

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
